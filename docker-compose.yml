services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: peduni
      POSTGRES_PASSWORD: peduni
      POSTGRES_DB: peduni
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U peduni"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
